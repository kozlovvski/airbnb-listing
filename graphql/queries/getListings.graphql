query getListings($limitAndOffset: GetListingsFuncInput) {
  listingsAndReviews(input: $limitAndOffset) {
    _id
    name
    summary
    price
    images {
      medium_url
      picture_url
      thumbnail_url
      xl_picture_url
    }
  }
}
